sas {
  master = "local[2]"
  appname = "SAS"

  parquet {
    path = "hdfs:///data/sandboxes/sas/in/parquet/vsfmahss.parquet"
    options {
    }
  }

  #CSV write options
  csv {
    path = "/tmp/output/csv/vsfmahss.csv",
    savemode = "overwrite"
    options {
    }
  }

}